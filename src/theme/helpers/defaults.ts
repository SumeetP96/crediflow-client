import { EFontNames, EThemeNames } from './types';

export const themeSettings = {
  borderRadius: 12,
};

export const fallBackFonts = 'Helvetica, Arial, sans-serif';

export const defaultFontName = EFontNames.ROBOTO;

export const defaultThemeName = EThemeNames.MATERIAL;
